🟦 Day1まとめ

今日は「バックエンドとは何か」を学んだ。
バックエンドは画面を作る場所ではなく、アプリのデータを正しく保存・処理・管理して、データの整合性を守る役割を持つと理解した。

人生ToDoアプリで言えば、「夢を保存する」「一覧を返す」「更新する」「削除する」といった裏側の処理をすべて担当する部分がバックエンドである。

フロント（React）は見える世界、バックエンド（Laravel）は見えない世界という役割分担があることを理解できた。